import "./styles.css";

const Header = () => {
    return (
        <header class="header">
            <h1>On Line Store</h1>
                <nav>
                    <a href="/login"> Login </a>
                </nav>
        </header>
    );
}

export default Header;